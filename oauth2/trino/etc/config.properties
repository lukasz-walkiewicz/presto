#
# WARNING
# ^^^^^^^
# This configuration file is for development only and should NOT be used
# in production. For example configuration, see the Presto documentation.
#

# sample nodeId to provide consistency across test runs
node.id=ffffffff-ffff-ffff-ffff-ffffffffffff
node.environment=test
node.internal-address=localhost
http-server.http.port=8080

discovery-server.enabled=true
discovery.uri=http://localhost:8080

exchange.http-client.max-connections=1000
exchange.http-client.max-connections-per-server=1000
exchange.http-client.connect-timeout=1m
exchange.http-client.idle-timeout=1m

scheduler.http-client.max-connections=1000
scheduler.http-client.max-connections-per-server=1000
scheduler.http-client.connect-timeout=1m
scheduler.http-client.idle-timeout=1m

query.client.timeout=5m
query.min-expire-age=30m

node-scheduler.include-coordinator=true

http-server.https.port=8443
http-server.https.enabled=true
http-server.https.keystore.path=/etc/trino/cert/localhost.pem

web-ui.enabled=true
web-ui.authentication.type=oauth2

http-server.authentication.type=oauth2
http-server.authentication.oauth2.auth-url=https://xxx.auth0.com/authorize
http-server.authentication.oauth2.token-url=https://xxx.auth0.com/oauth/token
http-server.authentication.oauth2.jwks-url=https://xxx.auth0.com/.well-known/jwks.json
http-server.authentication.oauth2.client-id=xxx
http-server.authentication.oauth2.client-secret=xxx
